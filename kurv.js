/*const kurv = [
    { produktnavn : "buks",
        antal : 1,
        pris : 1.125
    }
];

output = document .getElementsByClassName("antal")
for (i = 0; i < kurv.length;i++) {
let antal = '?' + kurv[i].link + '>' + kurv[i].antal + '?'
output.innerHTML+=buks
}*/

/*function addOneLoop(startNumber, times) {
    let result = startNumber; // Variablen defineres før loopet
    let i = 0; // Tællervariabel defineret udenfor loopet
    while (i < times) {
        result += 1;
        i++; // Øger tælleren for hver iteration
    }
    return result;
}

// Eksempel på brug
console.log(addOneLoop(1, 1)); // Output: 8
*/
